---
description: Review Pi setup status using the wiki and focused checks
argument-hint: "[area]"
---
<!-- Managed by modules/programs/cli/pi. Edit the source file there, not the generated runtime copy. -->

Review the Pi setup status for: $ARGUMENTS

Use focused checks. Start with:
- `modules/programs/cli/pi/docs/INDEX.md`
- `modules/programs/cli/pi/docs/LOOKUP.json`

Prefer `pi-admin status`, `pi-admin drift`, and `pi-admin compat` over broad repository scans.
Report only actionable findings.
