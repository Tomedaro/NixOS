---
description: Plan a safe Pi setup change using the setup wiki first
argument-hint: "<change request>"
---
<!-- Managed by modules/programs/cli/pi. Edit the source file there, not the generated runtime copy. -->


Help me change the Pi setup: $ARGUMENTS

Follow this route:
1. Read `modules/programs/cli/pi/docs/INDEX.md`.
2. Read `modules/programs/cli/pi/docs/LOOKUP.json`.
3. Identify the source-owned files to edit.
4. Explain whether runtime sync requires `pi-bootstrap`, `pi-study-init`, or `pi-work-init`.
5. Propose the smallest safe patch.
6. Tell me exact validation commands.
