# Pi Module — Structured Improvement Plan

Generated by: smarter LLM (Phase 2, after research brief)
Source: analysis of the Pi NixOS module tarball
Date: 2026-06-08

---

## Guiding principle

Keep current user-facing commands working, improve underneath in order:

```
verify → fail closed → expose options → reduce runtime drift → sandbox experiments → deeper supply-chain work
```

---

## Phase 0 — Baseline validation, no behavior change

### 0.1 Add source validation command — ✅ DONE
- New: `pi-admin source-check`
- Validates: JSON, JSONC, policy cross-refs, LOOKUP.json paths, wrapper resource refs, package spec parseability
- Files: scripts.nix, wrappers.nix, home-module.nix, docs
- Risk: Low
- Commits: `8c7e726`, `7ddd9a8`, `f966da2`

### 0.2 Add generated-script smoke checks — ✅ DONE
- Add to pi-doctor: verify all expected commands exist after switch
- Files: scripts.nix, docs
- Commit: `ae3870c`

---

## Phase 1 — Security hardening quick wins

### 1.1 Make all policy-backed profiles check permission extension presence — ✅ DONE
- Move check into wrapperPrelude
- Verify: policy exists, pi-permission-system installed, version matches pin
- Refuse launch on failure, escape hatch: pi-raw
- Files: wrappers.nix, scripts.nix, docs
- Commit: `98b5787`

### 1.2 Add narrow permission version check — ✅ DONE
- Shared extraction of pinned version from settings/global.json in wrapperPrelude
- Compares expected vs installed pi-permission-system version, refuses launch on mismatch
- Reused across all managed wrappers
- Files: wrappers.nix
- Commit: `98b5787`

### 1.3 Add pi-cautious as primary name — ❌ NOT STARTED
- New wrapper, same implementation as pi-readonly
- Keep pi-readonly and pi-safe as aliases
- Update docs to prefer pi-cautious / PI_PROFILE=cautious
- Files: wrappers.nix, home-module.nix, docs

### 1.4 Add friction to pi-trusted — ❌ NOT STARTED
- Require PI_TRUSTED_ACK=1 to proceed
- Print warning without it
- Files: wrappers.nix, docs

### 1.5 Add launch metadata stamping — ❌ NOT STARTED
- Write to ~/.pi/agent/sessions/<profile>/last-launch.json + launches.jsonl
- Metadata only: profile, timestamp, cwd, gitRoot, policy hash, versions
- Files: wrappers.nix, docs

---

## Phase 2 — Admin introspection and drift tooling

### 2.1 Add pi-admin explain-profile — ❌ NOT STARTED
- Output profile details: policy, session dir, settings, extension status, key policies
- Files: wrappers.nix, docs

### 2.2 Add pi-admin mcp-check — ❌ NOT STARTED
- Validate MCP config: file exists, matches source, no nix run github:, commands exist
- Files: scripts.nix, wrappers.nix, docs

### 2.3 Add pi-admin policy-lint — ❌ NOT STARTED
- Structural policy validation: profiles exist, JSONC parse, safe profile restrictions, trusted warnings
- Files: scripts.nix, docs

### 2.4 Add dry-run and diff to sync/drift — ❌ NOT STARTED
- pi-admin sync global --dry-run, pi-admin drift --diff
- Files: scripts.nix, wrappers.nix, docs

---

## Phase 3 — Minimal Home Manager module interface

### 3.1 Add programs.pi.enable — ❌ NOT STARTED
- Wrap in lib.mkIf cfg.enable
- Temporarily default true in local integration
- Files: home-module.nix, default.nix, docs

### 3.2 Expose core paths as options — ❌ NOT STARTED
- programs.pi.paths.* for agentDir, learningDir, workDir, nixosRepo, sourceDir
- Files: home-module.nix, lib/paths.nix, docs

### 3.3 Add basic assertions — ❌ NOT STARTED
- sourceDir exists, nixosRepo path required if nixos profile, learningDir required if study
- Files: home-module.nix, docs

---

## Phase 4 — Declarative global runtime

### 4.1 Activation-time drift warning — ❌ NOT STARTED
- Home Manager activation checks if global runtime differs from source
- Warning-only initially
- Files: home-module.nix, scripts.nix, docs

### 4.2 Activation-manage global files — ❌ NOT STARTED
- programs.pi.syncGlobalOnActivation option (default false)
- Run pi-bootstrap equivalent during activation
- Files: home-module.nix, scripts.nix, docs

### 4.3 Move immutable global files to home.file — ❌ NOT STARTED
- AGENTS.md, MEMORY.md, policies
- NOT settings.json, models.json, mcp.json yet
- Files: home-module.nix, scripts.nix, docs

---

## Phase 5 — MCP pinning

### 5.1 Add mcp-nixos as flake input/package — ❌ NOT STARTED
- Replace nix run github: with store path
- Files: mcp/global.json, home-module.nix, scripts.nix, package.nix, docs, parent flake

### 5.2 Make pi-admin mcp-check strict — ❌ NOT STARTED
- Fail on nix run github:, curl, wget, bash -c in MCP config
- Files: scripts.nix, docs

---

## Phase 6 — Package/runtime supply-chain tightening

### 6.1 Keep mutable npm, enforce exact versions — ❌ NOT STARTED
- Enforce exact versions for security-critical packages (permission-system, mcp-adapter)
- Warn for UI packages
- Files: wrappers.nix, scripts.nix, docs

### 6.2 Add package lock/stamp — ❌ NOT STARTED
- Source-managed package stamp: ~/.pi/agent/nix-managed-packages.json
- Files: scripts.nix, docs

### 6.3 Nix-package pi-permission-system — ❌ NOT STARTED
- Experiment with packaging permission extension through Nix
- Files: package.nix, wrappers.nix, docs

---

## Phase 7 — Shell and wrapper maintainability

### 7.1 Convert selected scripts to writeShellApplication — ❌ NOT STARTED
- Start with pi-npm, pi-source-check
- Files: package.nix, scripts.nix, wrappers.nix

### 7.2 Factor shared wrapper preflight — ❌ NOT STARTED
- Helper snippets: verify_policy_profile, write_launch_metadata, resolve_git_root
- Files: wrappers.nix

### 7.3 Move large shell bodies out of Nix strings — ❌ NOT STARTED
- To bin/*.sh with Nix substitution for paths
- Files: new bin/*.sh, scripts.nix, wrappers.nix, docs

---

## Phase 8 — Real sandbox as additive feature

### 8.1 Add experimental pi-sandbox — ❌ NOT STARTED
- bubblewrap-based: offline, read-only /nix, tmpfs home, empty /tmp
- Files: wrappers.nix, package.nix, home-module.nix, docs

### 8.2 Add sandbox regression tests — ❌ NOT STARTED
- Symlink escape, write outside workspace, network disabled
- Files: docs, possibly scripts.nix

### 8.3 Consider aliasing pi-readonly to sandbox later — ❌ NOT STARTED
- Only after pi-sandbox stable
- Files: wrappers.nix, docs

---

## Phase 9 — Documentation cleanup

### 9.1 Architecture diagram and ownership table — ❌ NOT STARTED
- docs/ARCHITECTURE.md or in RUNTIME_STATE.md
- Files: new doc, INDEX.md, LOOKUP.json

### 9.2 Threat model — ❌ NOT STARTED
- docs/THREAT_MODEL.md
- Files: new doc, INDEX.md, LOOKUP.json, SECURITY docs

### 9.3 Runbooks — ❌ NOT STARTED
- Update Pi binary, update extensions, recover runtime, rotate credentials, etc.
- Files: docs

---

## Bonus — pi-permission-system prompt debugging — ✅ DONE
- Added read-only bash patterns (ls, grep, cat, head, tail, etc.) to nixos policy
- Kept dangerous patterns denied, pi commands on ask
- Added rg denies for sensitive paths to match existing grep -R denies
- Files: policies/nixos.jsonc
- Commit: `8e24063`

---

## Dependency map (simplified)

```
0.x (validation) → supports everything
1.x (security) → blocks claiming profiles are safe
2.x (introspection) → supports user verification
3.x (HM module) → before serious reusable module work
4.x (declarative) → after drift/source checks reliable
5.x (MCP pinning) → security priority
6.x (supply-chain) → after version helper
7.x (refactors) → after validation/introspection
8.x (sandbox) → can start after 1.x stable
9.x (docs) → ongoing
```

## Do not do

- Full rewrite before validation
- Move study/work files to Home Manager
- Convert policies from JSONC to Nix attrsets
- Make shell deny patterns complete
- Remove pi-trusted
- Replace pi-readonly with bubblewrap immediately
- Package all npm extensions with Nix immediately
- Move to XDG paths now
- Automate provider metadata updates from web
